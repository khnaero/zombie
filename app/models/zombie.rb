class Zombie < ActiveRecord::Base
	# validates :name,
	# 					presence: true,
	# 					length: { minimum: 3 }

	# validates :graveyard,
	# 					presence: true,
	# 					length: { minimum: 3 }

	# validates :longitude,
	# 					presence: true,
	# 					length: { minimum: 3 }

	# validates :latitude,
	# 					presence: true,
	# 					length: { minimum: 3 }

end
